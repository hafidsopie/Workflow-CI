name: stunting-ci

entry_points:
  main:
    parameters:
      data_path:
        type: path
        default: stunting_wasting_preprocessed.csv
    command: "python modeling.py --data_path {data_path}"
